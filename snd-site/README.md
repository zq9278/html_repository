# SND UI Prototype

Static deployment package for `SND100_1024x600_UI_Prototype.html`.

Public entry:

```text
http://sanitlook.cn:18080/
```

## Docker

```bash
docker compose up -d --build
```

The container exposes the site on host port `18080`.
