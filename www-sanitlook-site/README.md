# Sanitlook Website

Static mirror of `www.sanitlook.cn`.

Public entry:

```text
http://sanitlook.cn:18081/
```

## Docker

```bash
docker compose up -d --build
```

The container exposes the site on host port `18081`.
